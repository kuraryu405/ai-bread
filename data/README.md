# 学習データ

`npm run organize-data` を実行すると、ユーザーが保管している写真を次のディレクトリへ**コピー**します。
元画像は変更・移動しません。

- `training/roll`: ロール 15 枚
- `training/twist`: ツイスト 15 枚
- `training/campagne`: カンパーニュ 14 枚（HEIC から JPEG へ変換）
- `evaluation/mixed`: 複数のパンが写る確認用画像 6 枚

画像本体は大きいため Git には含めません。
